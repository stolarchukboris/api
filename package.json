{
  "name": "api",
  "version": "1.0.0",
  "description": "My testing API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "pm2 start server.js -f && pm2 logs",
    "restart": "pm2 restart server.js && pm2 logs",
    "stop": "pm2 delete server.js"
  },
  "author": "stolarchukboris",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "knex": "^3.1.0",
    "mysql2": "^3.14.1"
  }
}
