{
  "name": "api",
  "version": "1.0.0",
  "description": "My testing API",
  "main": "./app/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "coldstart": "pnpm run build && pm2 start build/src/index.js -f && pm2 logs",
    "start": "pm2 start build/src/index.js -f && pm2 logs",
    "stop": "pm2 delete build/src/index.js && pm2 logs",
    "rebuild": "pnpm run build && pm2 restart build/src/index.js --update-env && pm2 logs"
  },
  "author": "stolarchukboris",
  "license": "MIT",
  "dependencies": {
    "@types/express": "^5.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "knex": "^3.1.0",
    "mysql2": "^3.14.1"
  }
}
